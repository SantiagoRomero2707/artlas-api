name: Test

on:
  pull_request:
    branches:
      - master

jobs:
  build-and-test:
    uses: SantiagoRomero2707/shared-workflows/.github/workflows/build.yml@master
    with:
      java-version: '17'