spring:
  application:
    name: artlas-api
  datasource:
    driverClassName: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-only
    properties:
      hibernate:
        format_sql: true
        hbm2ddl.create_namespaces: true