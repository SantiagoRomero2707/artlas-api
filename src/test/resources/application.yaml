spring:
  application:
    name: artlas-api
  datasource:
    driverClassName: org.postgresql.Driver
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        hbm2ddl.create_namespaces: true